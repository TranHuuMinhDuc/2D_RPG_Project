namespace Snorx.Enum
{
    public enum EnemyState
    {
        Idle,
        Chasing,
        Attacking,
        Die,
    }
    public enum PlayerState
    {
        Idle,
        Running,
        Attacking,
        Die,
    }
}

